import React from 'react';

const Slider = () => {
  return (
    <div style={{ width: '100%', height: '300px', background: '#ddd', display: 'flex', justifyContent: 'center', alignItems: 'center', marginBottom: '2rem' }}>
      <p>Slider Placeholder (Add images here)</p>
    </div>
  );
};

export default Slider;
